# Deployment-time connection identifiers only.
# Do not place database passwords, tokens, private keys, or connection strings here.
SYBASE_CONNECTION_ID=sybase-prod
SNOWFLAKE_CONNECTION_ID=snowflake-prod

# Optional non-secret deployment settings
MIGRATION_ENV=dev
MIGRATION_ARTIFACT_DIR=migrations/output
